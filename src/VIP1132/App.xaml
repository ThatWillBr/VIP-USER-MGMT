<Application x:Class="VIP1132.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Application.Resources>
    <Color x:Key="BackgroundColor">#020308</Color>
    <Color x:Key="SurfaceColor">#E00A0D15</Color>
    <Color x:Key="SurfaceRaisedColor">#F0101521</Color>
    <Color x:Key="BorderColor">#3DFFFFFF</Color>
    <Color x:Key="NeonCyanColor">#52F6FF</Color>
    <Color x:Key="NeonPinkColor">#FF3CCB</Color>
    <Color x:Key="NeonPurpleColor">#A855F7</Color>
    <Color x:Key="TextColor">#F7FAFF</Color>
    <Color x:Key="TextMutedColor">#93A4BD</Color>
    <Color x:Key="SuccessColor">#4CF0B5</Color>
    <Color x:Key="WarningColor">#FFD166</Color>
    <Color x:Key="DangerColor">#FF6B91</Color>

    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="SurfaceRaisedBrush" Color="{StaticResource SurfaceRaisedColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="NeonCyanBrush" Color="{StaticResource NeonCyanColor}" />
    <SolidColorBrush x:Key="NeonPinkBrush" Color="{StaticResource NeonPinkColor}" />
    <SolidColorBrush x:Key="NeonPurpleBrush" Color="{StaticResource NeonPurpleColor}" />
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" />
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}" />
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}" />

    <LinearGradientBrush x:Key="GlassCardBrush" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#EE111620" Offset="0" />
      <GradientStop Color="#DE090C13" Offset="0.52" />
      <GradientStop Color="#EA0D111B" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="GlassButtonBrush" StartPoint="0,0" EndPoint="0,1">
      <GradientStop Color="#F0222A3A" Offset="0" />
      <GradientStop Color="#EF111722" Offset="0.5" />
      <GradientStop Color="#F00A0D14" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="NeonGradientBrush" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#45F3FF" Offset="0" />
      <GradientStop Color="#8A78FF" Offset="0.48" />
      <GradientStop Color="#FF3CCB" Offset="1" />
    </LinearGradientBrush>

    <Style TargetType="Window">
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="Background" Value="{StaticResource BackgroundBrush}" />
      <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
      <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <Style x:Key="GlassCard" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource GlassCardBrush}" />
      <Setter Property="BorderBrush" Value="#36FFFFFF" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="18" />
      <Setter Property="Padding" Value="20" />
      <Setter Property="Effect">
        <Setter.Value>
          <DropShadowEffect Color="#000000" BlurRadius="28" ShadowDepth="12" Opacity="0.72" />
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="GlassButton" TargetType="Button">
      <Setter Property="Foreground" Value="#F7FAFF" />
      <Setter Property="Background" Value="{StaticResource GlassButtonBrush}" />
      <Setter Property="BorderBrush" Value="#45FFFFFF" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="17,12" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="HorizontalContentAlignment" Value="Center" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Grid>
              <Border x:Name="Glow" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="11" Padding="{TemplateBinding Padding}">
                <Grid>
                  <Border Height="1" VerticalAlignment="Top" Margin="7,-1,7,0" CornerRadius="1" Background="#72FFFFFF" />
                  <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" />
                </Grid>
              </Border>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Glow" Property="BorderBrush" Value="#B852F6FF" />
                <Setter TargetName="Glow" Property="Background" Value="#F01B2737" />
                <Setter TargetName="Glow" Property="Effect">
                  <Setter.Value>
                    <DropShadowEffect Color="#52F6FF" BlurRadius="24" ShadowDepth="0" Opacity="0.78" />
                  </Setter.Value>
                </Setter>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Glow" Property="Opacity" Value="0.88" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Glow" Property="Opacity" Value="0.38" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="PrimaryGlassButton" TargetType="Button" BasedOn="{StaticResource GlassButton}">
      <Setter Property="Background" Value="{StaticResource NeonGradientBrush}" />
      <Setter Property="BorderBrush" Value="#D9FFFFFF" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="Padding" Value="23,14" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Grid>
              <Border x:Name="Glow" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="1" CornerRadius="11" Padding="{TemplateBinding Padding}">
                <Border.Effect>
                  <DropShadowEffect Color="#A855F7" BlurRadius="18" ShadowDepth="0" Opacity="0.46" />
                </Border.Effect>
                <Grid>
                  <Border Height="1" VerticalAlignment="Top" Margin="8,-1,8,0" Background="#B5FFFFFF" />
                  <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
              </Border>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Glow" Property="Effect">
                  <Setter.Value>
                    <DropShadowEffect Color="#FF3CCB" BlurRadius="32" ShadowDepth="0" Opacity="0.9" />
                  </Setter.Value>
                </Setter>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Glow" Property="Opacity" Value="0.86" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Glow" Property="Opacity" Value="0.38" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="MicroLabel" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#7F93AF" />
      <Setter Property="FontSize" Value="10" />
      <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style x:Key="NeonProgress" TargetType="ProgressBar">
      <Setter Property="Height" Value="7" />
      <Setter Property="Background" Value="#FF141A25" />
      <Setter Property="Foreground" Value="{StaticResource NeonGradientBrush}" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ProgressBar">
            <Grid x:Name="PART_Track" ClipToBounds="True">
              <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderBrush="#28FFFFFF" BorderThickness="1" />
              <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="4" Background="{TemplateBinding Foreground}">
                <Border.Effect>
                  <DropShadowEffect Color="#52F6FF" BlurRadius="8" ShadowDepth="0" Opacity="0.8" />
                </Border.Effect>
              </Border>
              <Rectangle x:Name="ActivityGlow" Width="110" HorizontalAlignment="Left" Opacity="0" IsHitTestVisible="False">
                <Rectangle.Fill>
                  <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop Color="#0052F6FF" Offset="0" />
                    <GradientStop Color="#C8FFFFFF" Offset="0.5" />
                    <GradientStop Color="#0052F6FF" Offset="1" />
                  </LinearGradientBrush>
                </Rectangle.Fill>
                <Rectangle.RenderTransform>
                  <TranslateTransform x:Name="ActivityTranslate" X="-140" />
                </Rectangle.RenderTransform>
              </Rectangle>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="Tag" Value="Active">
                <Setter TargetName="ActivityGlow" Property="Opacity" Value="0.78" />
                <Trigger.EnterActions>
                  <BeginStoryboard x:Name="ProgressActivityStoryboard">
                    <Storyboard RepeatBehavior="Forever">
                      <DoubleAnimation Storyboard.TargetName="ActivityTranslate"
                                       Storyboard.TargetProperty="X"
                                       From="-140" To="1200"
                                       Duration="0:0:1.65" />
                    </Storyboard>
                  </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                  <StopStoryboard BeginStoryboardName="ProgressActivityStoryboard" />
                </Trigger.ExitActions>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ScrollBar">
      <Setter Property="Width" Value="8" />
      <Setter Property="Background" Value="#080B11" />
      <Setter Property="Opacity" Value="0.75" />
    </Style>
  </Application.Resources>
</Application>
